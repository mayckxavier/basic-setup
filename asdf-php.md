# Ubuntu 22.04

```bash
apt install re2c  autoconf  bison  libxml2 libxml2-dev openssl libssl3 libssl-dev libsqlite3-dev zlib1g-dev zlib1g build-essential autoconf libtool bison re2c pkg-config libcurl4-openssl-dev libgd-dev libonig-dev libpq-dev libreadline-dev libzip-dev
```
